# ShooterHTML

Shooter-Spiel als einzelne, selbst-enthaltene HTML-Datei.
Vanilla JS + Canvas, keine Abhängigkeiten.

## Starten

```bash
open shooter.html
# oder Doppelklick in Finder
```

Funktioniert direkt vom Dateisystem — kein Server nötig.
